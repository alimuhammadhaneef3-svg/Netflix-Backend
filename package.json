{
  "name": "netflix-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "axios": "^1.4.0"
  }
}
```

**Step 2 - Delete Procfile in VSCode terminal:**
```
del Procfile
```

**Step 3 - Remove node_modules again:**
```
git rm -r --cached node_modules
```

**Step 4 - Push:**
```
git add .
git commit -m "clean fix"
git push origin master --force